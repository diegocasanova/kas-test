spring:
  application:
    name: kas-test

swagger:
  enabled: true

kas-test:
  packages:
    rest:
      baseUrl: http://opendata-ajuntament.barcelona.cat/
    service:
      defaultOffset: 0
      defaultLimit: 10
    api:
      defaultLang: ca
